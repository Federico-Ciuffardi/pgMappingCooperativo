controller_frequency: 5

recovery_behaviors: [{name: rotate_recovery, type: rotate_recovery/RotateRecovery}]

base_global_planner: "carrot_planner/CarrotPlanner"

base_local_planner: "teb_local_planner/TebLocalPlannerROS" 
TebLocalPlannerROS:
  # Robot Configuration Parameters
  max_vel_x: 0.75
  max_vel_y: 0.75
  max_vel_theta: 1.0

  max_vel_x_backwards: 0.75

  acc_lim_theta: 1.5
  acc_lim_x: 0.5
  acc_lim_y: 0.5

  footprint_model/type : "point" # footprint already taken into account with the inflation of the costmap 

  # Obstacle Parameters
  min_obstacle_dist: 1.0

  # Goal Tolerance Parameters
  yaw_goal_tolerance: 360
  xy_goal_tolerance: 0.5
  free_goal_vel: true

  # Optimization Parameters
  weight_kinematics_forward_drive: 1.0

  # Parallel Planning in distinctive Topologies
  enable_homotopy_class_planning: false

# base_local_planner: "eband_local_planner/EBandPlannerROS" 
# EBandPlannerROS:
# ...
